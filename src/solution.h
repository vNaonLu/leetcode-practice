#include <iostream>
#include <string>
#include <vector>

class Solution{
  public:
  // 0022
    static std::vector<std::string> generateParenthesis(int parenthesisCount);
    static void _0022_solveGenerateParenthesis(int parenthesisCount);
};